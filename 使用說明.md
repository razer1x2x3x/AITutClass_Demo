# 光的旅行｜可執行 Prototype

## 直接開始

最簡單：開啟 `dist/index.html`。不需安裝，所有實驗、證據、作品、測驗、教師頁和來源頁均可操作。

也可雙擊 `啟動原型.cmd`，再於瀏覽器開啟畫面顯示的本機網址（預設 http://127.0.0.1:4174）。此方式需 Node.js，亦會尋找這台電腦的 Codex 附帶 Node.js。關閉啟動視窗即可停止服務。

## 建議示範順序

1. 留下「光進水一定轉彎」的猜想與理由。
2. 選水、35°，選預測，顯示光路並保存。
3. 保持水不變，改 0°，再預測、揭示、保存。
4. 按「給我提示」，看具體比較方式。
5. 到「比較與解釋」，比較 E01、E02，保存改變的條件與光路觀察。完整解釋在「我的發現」填寫，按「確定」保存，按「編輯」修改。
6. 進入光路設計；可先用長方形、0°、高度與目標相同，保存成功結果，再換三角形挑戰。
7. 到「好奇再出發」，進入顏色偵探。重設器材後，用白光保存一次，只改紅光再保存，完成發現卡。
8. 在「好奇再出發」第 04 區塊完成 10 題「學習驗收」，回顧折射、色散、虛像與比較方法。
9. 查看「我的發現」與「教師視角」，閱讀逐題驗收結果、列印或匯出紀錄。
10. 開啟「來源與查核」，查看企劃全部來源及原型限制。

## 保存與重新演示

- 紀錄僅存在目前瀏覽器與網址，不會同步不同瀏覽器、裝置、網址或連接埠。
- 本機版、直接開啟檔案版與線上版，彼此紀錄分開。
- 文字草稿會儲存，但提交後才算作品／解釋里程碑。
- 教師頁可以先匯出，再確認重設。所有實驗合計最多保留 20 張證據小卡、250 個近期事件。刪除小卡後可以再存，復原也遵守 20 張上限；更新前已有超過上限的舊紀錄不會自動刪除。
- 匯出 JSON 適合保存原始資料；「列印」可以另存 PDF。原型沒有匯入或正式班級帳號管理。

## 引導模式

**預設引導：**直接開啟檔案或尚未設定 AI 時，根據材料、方向、已保存證據與問題類型給出規則提示，清楚標示為預設引導。網站的線上靜態版也使用此模式。

**本機 AI 接口：**附帶的 `server.mjs` 可呼叫 OpenAI Responses API。金鑰只留在服務端環境，不輸入學生頁面、不保存進瀏覽器。

由管理者在啟動服務前設定 `OPENAI_API_KEY` 與 `OPENAI_MODEL`（使用帳號可用且支援 Responses API 結構化輸出的模型），再執行 `node server.mjs`。這是選配；本次沒有取得金鑰，未實測外部模型的即時回覆或成本。AI 服務可能產生帳號費用。

提交「提出我的問題」時會向已設定的 AI 服務傳送問題、目前配置、原始猜想及最多六筆相關證據。請勿在原型輸入學生真實個資。模型產出必須引用存在的證據 ID，否則改用預設引導；超時、錯誤或未設定也會自動降級。

「給我下一個線索」及「看一個具體做法」固定採可重現的預設提示。AI 即時回答仍需學科審閱，引用驗證不等同內容正確性保證。

## 檔案與擴充

- `dist/`：可獨立執行的網站，含所有頁面、光學模型與來源清單。
- `dist/unit-package.json`：教材單位接續製作的示範結構，外部未核實代碼保留 null 與待審狀態。
- `server.mjs`：免額外套件的 Node.js 本機伺服器與可設定 AI 接口；僅綁定本機。
- `驗證紀錄.md`：本次已檢查項目與未驗證範圍。

內容來源與查核範圍以網站「來源與查核」頁為準。本原型已依使用者操作測試回饋調整，尚未完成正式教材、學科專家或學習成效驗證。
# 2026-09-17 互動更新

- 主色依 ThemeColor.png 更新為深藍、電光藍、青、金、白。
- 顏色偵探：先選積木，再拖曳手電筒移動、拖曳旋轉圖示轉向；也可聚焦手電筒或旋轉圖示後使用方向鍵微調。
- 清楚展開七色光線後會出現「彩虹出現了」，可點開教學說明與動畫。窄螢幕可左右滑動實驗區查看出光位置。
- 曲度拉桿中央附近會吸附為平直玻璃；鍵盤方向鍵維持逐格微調。
- 「查看我的發現」入口位於「好奇再出發」的下一步卡片。
- 開發驗證：在 light-lab 執行 `node --test tests/optics.test.cjs tests/assessment.test.cjs tests/flow.test.cjs`。

## 2026-09-18 學習驗收

- 「先猜猜看」移除暖身題入口；舊暖身網址回到先猜猜看。
- 「新情境挑戰」改為「學習驗收」，入口移到「好奇再出發」第 04 區塊。整理「我的發現」的下一步按鈕保留在四張活動卡下方。
- 共 10 題單選題，未完成全部題目不能提交。作答草稿自動保存；提交後顯示總分、正確答案及逐題解析。
- 教師視角顯示最近一次驗收結果與需再確認的概念，並保留修改前的作答及舊版三題測驗紀錄。重新修改答案後，須再提交才會更新教師頁的成績。
- 本機紀錄依瀏覽器、網址及裝置分開；老師需使用同一份紀錄，或由學生匯出分享，並非自動收集全班成績。

## 使用者測試後調整

- 折射實驗顯示「光會轉彎嗎？」與「調整光線的角度」。儲存兩個不同條件的折射結果後，才能進入比較；同條件重複儲存不會新增小卡。
- 儲存區顯示 0/2、1/2、2/2。未達門檻的下一步按鈕呈灰色，滑鼠移上或鍵盤聚焦外框可閱讀提示。側欄與直接網址也遵守相同門檻。
- 比較頁移除教練欄位，只保存兩次實驗的比較。我的發現提供「我現在認為」的確認、編輯與取消編輯，編輯草稿可保留，教師只讀取已確認文字。
- 挑戰 A 位於右方；新版挑戰 B 位於積木下方。命中 B 且光路確實經過全反射時，可點圖中的小對話框閱讀圖文說明。舊證據仍按原目標位置重現。
- 彩虹提示放寬為可辨識的七色光束，對話框指向彩色光路；單色、未穿過積木或不在可視範圍的色光不觸發。窄螢幕可橫向滑動實驗區。
- 詳細流程與矛盾修正見「架構檢查_2026-09-18.md」。


## 2026-09-21：AI 與成人試用準備

加入比較、驗收與教師教學建議三處 AI 分析接口，需設定服務端 API 金鑰和模型後才能啟用。驗收頁有完整五步驟導覽。我的發現／教師視角可下載可直接閱讀的 HTML 教師摘要。部署設定與需要提供的帳號請見「上線與AI啟用清單.md」。本次尚未對外上線，也尚未驗證真實模型回覆。
