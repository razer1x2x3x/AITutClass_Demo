{
  "name": "light-journey-trial",
  "private": true,
  "version": "1.3.0",
  "engines": {"node": ">=22"},
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.cjs tests/*.test.mjs"
  }
}
